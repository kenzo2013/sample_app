<h1>Tous les utilisateurs</h1>
	
	<ul class="users">
		<% @users.each do |user| %>
		<li>
			<%= gravatar_for user, :size => 5 %>
			<%= link_to user.name, user %>
			<% if current_user.admin? %> | <%= link_to "supprimer", user, :method => :delete, :confirm => "Etes- vous 					certain&nbsp;?", :title => "Supprimer #{user.name}" %>
			<% end %>
		</li>
		
	</ul>
	
